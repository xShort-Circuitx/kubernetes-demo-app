{
  "name": "kubernetes-demo-app",
  "version": "1.0.0",
  "description": "A simple web application for Kubernetes deployment",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
} 